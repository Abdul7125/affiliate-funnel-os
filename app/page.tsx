import { entryList, offers } from './content-data';
import { Footer, Header } from './site-shell';

export default function Home() { return <main><Header /><section className="os-home shell"><div><p className="eyebrow">Affiliate Funnel OS</p><h1>One funnel system for five different starting questions.</h1><p className="lede">Teach first. Capture a useful signal. Branch only when intent is clear. This starter system is built for ClickFunnels affiliate routing without pretending the affiliate destinations are live before they are configured.</p><div className="hero-actions"><a className="primary-button" href="/calculator">Start with the calculator →</a><a className="secondary-button" href="/admin">Open the demo dashboard</a></div></div><aside className="home-note"><span>System rule</span><strong>Educational assets 1–3 are shared.</strong><p>Asset 4 asks what the visitor is building, then selects the nurture and recommendation path.</p></aside></section><section className="entry-grid-section"><div className="shell"><div className="section-heading"><div><p className="eyebrow">Five entry pages</p><h2>Meet the visitor at the problem they already named.</h2></div><p>Every page uses the same capture, tracking, tag, consent, and routing engine.</p></div><div className="entry-grid">{entryList.map((entry) => <a href={entry.path} key={entry.key}><span>{entry.tag}</span><h3>{entry.title}</h3><p>{entry.promise}</p><b>Open entry →</b></a>)}</div></div></section><section className="offer-section"><div className="shell"><div className="section-heading"><div><p className="eyebrow">Offer routing</p><h2>Five configured destinations, zero invented URLs.</h2></div><p>Each offer remains disabled until its environment placeholder is replaced with an approved affiliate URL.</p></div><div className="offer-mini-grid">{Object.values(offers).map((offer) => <article key={offer.key}><span>{offer.key}</span><h3>{offer.name}</h3><p>{offer.audience}</p><small>Config: {offer.envKey}</small></article>)}</div></div></section><Footer /></main>; }
